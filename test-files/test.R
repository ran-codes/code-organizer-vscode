# 1. Data Analysis ----
library(ggplot2)
library(dplyr)
code
code
code
code
code
code
code
code
code
code
code

## 1.1 Data Loading ----
data <- read.csv("data.csv")
code
code
code
code
code
code
code
code

## 1.2 Plotting ----
plot <- ggplot(data, aes(x, y)) + geom_point()
code
code
code
code
code
code
code
code
code
code
code
code
code
code
code

# 2. Statistics ----
mean_value <- mean(data$value)
code
code
code
code
code
code
code
code
code
code
code
code
code

## 2.1 Summary ----
summary(data)
code
code
code
code
code
code
code
